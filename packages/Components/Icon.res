module CircleX = {
  @react.component @module("@tabler/icons")
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~stroke: int=?,
    ~className: string=?,
  ) => React.element = "IconCircleX"
}
module AlertCircle = {
  @react.component @module("@tabler/icons")
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~stroke: int=?,
    ~className: string=?,
  ) => React.element = "IconAlertCircle"
}
module InfoCircle = {
  @react.component @module("@tabler/icons")
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~stroke: int=?,
    ~className: string=?,
  ) => React.element = "IconInfoCircle"
}
module CircleCheck = {
  @react.component @module("@tabler/icons")
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~stroke: int=?,
    ~className: string=?,
  ) => React.element = "IconCircleCheck"
}
module X = {
  @react.component @module("@tabler/icons")
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~stroke: int=?,
    ~className: string=?,
  ) => React.element = "IconX"
}
